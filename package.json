{
  "name": "rl-demo",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "format:check": "prettier --check .",
    "format:write": "prettier --write .",
    "start": "ts-node src/main.ts",
    "build": "tsc -p tsconfig.json",
    "start:prod": "npm run build && node dist/main.js",
    "run:real": "ts-node src/run/real.ts",
    "run:shadow": "ts-node src/run/shadowTraining.ts",
    "docs:generate": "typedoc --out docs/api src --entryPoints src/index.ts",
    "docs:serve": "npx http-server docs/api -p 8080 -o",
    "docs:clean": "rm -rf docs/api",
    "docs:build": "npm run docs",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs": "typedoc --options typedoc.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@tensorflow/tfjs": "^4.22.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.4",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typedoc": "^0.28.13",
    "typescript": "^5.9.2"
  }
}
